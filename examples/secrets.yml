# Example secrets file (plainfile backend). Protect with OS permissions,
# or use the encryptedfile backend for at-rest encryption.
core-sw-01:
  username: backup
  password: change-me
  # enable_secret: change-me-too
